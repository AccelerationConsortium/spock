
# Goal: structure MD file and documents in general / maybe json like the doc
import typing
class Document:
    def __init__(self):
        pass
    
    @staticmethod
    def serialize(self, document) -> dict:
        document = Document(document)
        reutn
    